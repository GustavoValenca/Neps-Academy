#include <bits/stdc++.h>

using namespace std;

int main(){
    double x, y;
    cin >> x >> y;

    // cout.precision(4);
    // cout.setf(ios::fixed);

    cout << fixed << setprecision(4) << pow(x, y) << endl;

    return 0;
}
